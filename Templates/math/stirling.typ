=== 斯特林数

本节统一使用以下记号：

- $c(n,k)$：无符号第一类斯特林数，也叫轮换数。
- $s(n,k)=(-1)^(n-k)c(n,k)$：有符号第一类斯特林数。
- $S(n,k)$：第二类斯特林数，也叫子集数。
- $L(n,k)$：无符号 Lah 数。

边界统一为 $c(0,0)=S(0,0)=1$，当 $k<0$ 或 $k>n$ 时取 $0$。

==== 自然幂、上升幂和下降幂

自然幂、上升幂、下降幂分别记作：

$
x^n=product_(i=0)^(n-1)x,quad
x^(overline(n))=product_(i=0)^(n-1)(x+i),quad
x^(underline(n))=product_(i=0)^(n-1)(x-i)
$

上升幂和下降幂之间可以互相转化：

$
(-x)^(overline(n))=(-1)^n x^(underline(n)),quad
(-x)^(underline(n))=(-1)^n x^(overline(n))
$

这三种幂构成三组常用基。斯特林数本质上就是它们之间的换基系数。

=== 第一类斯特林数

==== 组合意义

$c(n,k)$ 表示将 $n$ 个两两不同的元素划分成 $k$ 个互不区分的非空轮换的方案数。

轮换是首尾相接的环形排列。两个轮换如果只相差整体旋转，则视为相同；如果相差翻转，则不视为相同。

==== 递推式

$
c(n,k)=c(n-1,k-1)+(n-1)c(n-1,k)
$

证明思路：加入第 $n$ 个元素时，它要么单独形成一个新轮换，要么插入已有轮换。已有 $n-1$ 个位置可插入，所以第二项乘上 $n-1$。

==== 生成函数与幂转换

无符号第一类斯特林数满足：

$
x^(overline(n))=sum_(k=0)^n c(n,k)x^k
$

有符号第一类斯特林数满足：

$
x^(underline(n))=sum_(k=0)^n s(n,k)x^k
=sum_(k=0)^n (-1)^(n-k)c(n,k)x^k
$

同一列的指数型生成函数为：

$
sum_(n>=0)c(n,k) x^n/n! = 1/(k!) (ln(1/(1-x)))^k
$

==== 计算同一行

令

$
F_n(x)=product_(i=0)^(n-1)(x+i)
$

则 $[x^k]F_n(x)=c(n,k)$。

分治计算时：

$
F_(2n)(x)=F_n(x)F_n(x+n)
$

若 $n$ 为奇数，再乘上 $x+n-1$。其中 $F_n(x+n)$ 可以用多项式平移完成。

复杂度：使用 NTT 和快速平移时为 $O(n log n)$ 级别。

==== 计算同一列

固定 $k$ 时，由指数型生成函数：

$
c(n,k)=n! [x^n] 1/(k!) (ln(1/(1-x)))^k
$

代码中将 $ln(1/(1-x))=sum_(i>=1) x^i/i$ 截断到 $n$ 项后做多项式幂，再乘 $n!/k!$。

=== 第二类斯特林数

==== 组合意义

$S(n,k)$ 表示将 $n$ 个两两不同的元素划分为 $k$ 个互不区分的非空集合的方案数。

==== 递推式

$
S(n,k)=S(n-1,k-1)+k S(n-1,k)
$

证明思路：加入第 $n$ 个元素时，它要么单独形成一个新集合，要么放入已有的 $k$ 个集合之一。

==== 幂转换

第二类斯特林数满足：

$
x^n=sum_(k=0)^n S(n,k)x^(underline(k))
$

由上升幂和下降幂的关系，也有：

$
x^n=sum_(k=0)^n (-1)^(n-k)S(n,k)x^(overline(k))
$

==== 通项公式

$
S(n,k)=1/(k!) sum_(i=0)^k (-1)^(k-i) binom(k,i)i^n
$

等价地：

$
S(n,k)=sum_(i=0)^k ((-1)^(k-i)i^n)/(i!(k-i)!)
$

证明可由容斥得到：先把 $n$ 个元素映射到 $k$ 个有标号盒子，再排除空盒。

==== 计算同一行

固定 $n$，通项公式可以写成一次卷积：

$
S(n,k)=sum_(i=0)^k (-1)^(k-i)/(k-i)! dot i^n/i!
$

因此构造

$
A_i=(-1)^i/i!,quad B_i=i^n/i!
$

则 $S(n,k)=[x^k] A(x)B(x)$。

复杂度：$O(n log n)$。

==== 计算同一列

固定 $k$ 时，指数型生成函数为：

$
sum_(n>=0)S(n,k)x^n/n! = 1/(k!)(e^x-1)^k
$

代码中将 $e^x-1=sum_(i>=1)x^i/i!$ 截断到 $n$ 项后做多项式幂，再乘 $n!/k!$。

=== Lah 数

==== 组合意义

无符号 Lah 数 $L(n,k)$ 表示将 $n$ 个两两不同的元素划分为 $k$ 个非空线性有序块的方案数。块之间互不区分，块内有顺序。

==== 通项公式

$
L(n,k)=binom(n-1,k-1) n! / k!
$

==== 幂转换

Lah 数连接上升幂和下降幂：

$
x^(overline(n))=sum_(k=0)^n L(n,k)x^(underline(k))
$

$
x^(underline(n))=sum_(k=0)^n (-1)^(n-k)L(n,k)x^(overline(k))
$

==== 递推式

$
L(n,k)=(n+k-1)L(n-1,k)+L(n-1,k-1)
$

边界为 $L(0,0)=1$。

=== 斯特林反演

第一类和第二类斯特林数是互逆的换基矩阵。用无符号第一类斯特林数表示时，反演式中会出现符号。

若

$
f_n=sum_(k=0)^n S(n,k)g_k
$

则

$
g_n=sum_(k=0)^n (-1)^(n-k)c(n,k)f_k
$

若

$
f_n=sum_(k=0)^n c(n,k)g_k
$

则

$
g_n=sum_(k=0)^n (-1)^(n-k)S(n,k)f_k
$

矩阵形式为：

$
sum_(i=k)^n (-1)^(n-i)c(n,i)S(i,k)=[n=k]
$

$
sum_(i=k)^n (-1)^(n-i)S(n,i)c(i,k)=[n=k]
$

=== 代码接口

`stirling.hpp` 中提供四个函数：

```cpp
stirling::s1row(n);     // 返回 c(n,0), c(n,1), ..., c(n,n)
stirling::s1col(n, k);  // 返回 c(0,k), c(1,k), ..., c(n,k)
stirling::s2row(n);     // 返回 S(n,0), S(n,1), ..., S(n,n)
stirling::s2col(n, k);  // 返回 S(0,k), S(1,k), ..., S(n,k)
```

这些函数依赖 `polynomial.hpp` 中的多项式乘法、平移和快速幂。输入规模较大时，优先使用同一行或同一列函数，不要用二维递推表。

=== 三类数的关系图

#image("jYZpd0.png")
