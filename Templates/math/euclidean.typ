=== 数学形式

类欧几里得算法处理的一类基本对象是
$
  S(a,b,c,n) = sum_(i=0)^n floor((a i + b) / c)
$
其中通常要求 $c > 0$。递归的核心和普通欧几里得算法一样：不断把较大的参数拆掉，直到问题规模下降。

当 $a >= c$ 或 $b >= c$ 时，直接拆出整数部分：
$
  floor((a i + b) / c)
  = floor(a / c) i + floor(b / c)
    + floor(((a mod c) i + (b mod c)) / c)
$
所以
$
  S(a,b,c,n)
  = floor(a / c) n(n+1) / 2
    + floor(b / c)(n+1)
    + S(a mod c, b mod c, c, n)
$

当 $0 <= a < c$ 且 $0 <= b < c$ 时，令
$
  m = floor((a n + b) / c)
$
若 $m = 0$，答案为 $0$。否则把格点按横纵坐标互换计数，可得
$
  S(a,b,c,n) = n m - S(c, c - b - 1, a, m - 1)
$
这个式子把模数从 $c$ 换成了 $a$，因此递归深度与欧几里得算法同阶。

=== 万能欧几里得

万能欧几里得把上面的递归抽象成两个操作序列：

- $U$：处理 $a >= c$ 或 $b >= c$ 时拆出的整段贡献。
- $R$：处理 $0 <= a,b < c$ 时的互换递归。

如果需要的不只是 $sum floor((a i+b)/c)$，而是某个可以按操作序列组合的量，就把每个 $U$ 和 $R$ 看成幺半群元素。递归过程中只负责产生这些元素，并用乘法合并。这样可以统一维护一次项、二次项、交叉项，或者题目中自定义的转移矩阵。

模板中的 `node` 存储当前操作对应的矩阵，`operator*` 表示操作复合；`euclid(p,q,r,l,U,R)` 会根据类欧几里得递归返回一段操作序列的乘积。只要把 $U,R$ 设成目标问题的基础转移，递归结构就不需要改变。
