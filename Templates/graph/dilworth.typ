== Dilworth 定理与 Mirsky 定理

设 $(P,≼)$ 是含 $n$ 个元素的有限偏序集。*链*中的元素两两可比，*反链*中的不同元素两两不可比；链划分、反链划分都要求把每个元素恰好放入一组。

=== 两个极值定理

- *Dilworth 定理：*
$
  op("最大反链大小")=op("最少链划分数")=op("width")(P).
$

- *Mirsky 定理：*
$
  op("最大链大小")=op("最少反链划分数")=op("height")(P).
$

=== Dilworth 的二分图算法

建立左右各一份 $P$ 的二分图；对每个严格关系 $x ≺ y$ 连边 $x_L-y_R$。若最大匹配大小为 $M$，则
$
  op("最少链划分数")=n-M=op("最大反链大小").
$
每条匹配边把同一条链中的前驱、后继接起来；每个点至多匹配一个前驱和一个后继，最终得到若干条互不相交的链。可用 König 定理由最大匹配恢复一组最大反链。

*传递闭包边界：* 若偏序由 DAG 的可达关系定义，为求任意链划分，二分图必须对所有可达对 $x ≺ y$ 连边，即使用传递闭包。只用原 DAG 边算出的是“沿原边走”的最小不交路径覆盖；两者不要混用。

=== 对偶定理的构造

令
$
  d(x)=1+max_(y ≺ x) d(y),
$
无前驱时 $d(x)=1$。$max_x d(x)=op("height")(P)$；把 $d(x)$ 相同的元素放在一组，同组内不可能存在可比关系，因此得到恰好 $op("height")(P)$ 组反链。这同时给出 Mirsky 定理的构造性证明。
