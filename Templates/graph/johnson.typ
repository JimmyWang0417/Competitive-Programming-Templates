=== Johnson 多源最短路 <johnson-potential>

设有向图有 $n$ 个点、$m$ 条边。Johnson 算法用于求任意两点间的最短路。它允许负边，但图中不能有负环。

==== 势能重标

新建超级源点 $s$，从 $s$ 向每个原图顶点连接一条权值为 $0$ 的边。用 Bellman–Ford 求 $s$ 到各点的最短距离 $h(v)$；如果检测到负环，算法停止。实现时可以直接把所有 $h(v)$ 初始化为 $0$，不必真的加入这些边。

对每条边 $(u,v)$，把边权改为

$ w'(u,v)=w(u,v)+h(u)-h(v). $

最短路满足 $h(v) <= h(u)+w(u,v)$，所以 $w'(u,v)>=0$，可以对每个起点运行 Dijkstra。

一条从 $u$ 到 $v$ 的路径经过重标后，只有两个端点的势能没有抵消，因此原图距离为

$ d(u,v)=d'(u,v)-h(u)+h(v). $

下面的代码只包含重标这一步。使用前需要让 `dis[v]` 保存 $h(v)$：

#raw(read("johnson.hpp"), lang: "cpp", block: true)

使用二叉堆时，Bellman–Ford 需要 $O(n m)$ 时间；$n$ 次 Dijkstra 共需 $O(n (m+n) log n)$ 时间。不保存答案矩阵时，额外空间为 $O(n+m)$；保存所有点对距离时还需要 $O(n^2)$ 空间。

==== 在费用流中的用法

每次沿费用最小的增广路增加流量时，也可以给每个点维护势能 $p$，把残量边费用改写为

$ c'(u,v)=c(u,v)+p(u)-p(v). $

$c'$ 是重标后的费用，也称约化费用。若所有可用残量边的 $c'$ 非负，就可以用 Dijkstra 求下一条最短增广路。设这次 Dijkstra 求得的距离为 $delta(v)$，对所有可达点更新 $p(v) <- p(v)+delta(v)$，下一轮的约化费用仍然非负。若初始残量网络含负费用边，需要先用 Bellman–Ford 或 SPFA 求初始势能。

这里使用的是与 Johnson 相同的重标方法，不需要求所有点对最短路。
