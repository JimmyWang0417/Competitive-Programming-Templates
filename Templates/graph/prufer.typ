=== Prüfer 序列

设一棵树有 $n>=2$ 个点，编号为 $1,2,dots,n$。它的 Prüfer 序列有 $n-2$ 项，每一项都是一个顶点编号。

==== 编码

每次找到编号最小的叶子，把与它相邻的点加入序列，然后删除这个叶子。重复 $n-2$ 次，最后剩下两个点。$n=2$ 时序列为空。

==== 从序列还原树

点 $i$ 的初始度数等于它在序列中的出现次数加 $1$。依次处理序列中的每一项 $x$：找到当前编号最小且度数为 $1$ 的点 $u$，加入边 $(u,x)$，然后把 $u$ 和 $x$ 的度数各减 $1$。序列处理完后，再连接剩下的两个度数为 $1$ 的点。

编码和还原都可以用小根堆维护编号最小的叶子，时间复杂度为 $O(n log n)$，空间复杂度为 $O(n)$。

==== 常用结论

- 每棵带标号树对应唯一的 Prüfer 序列，每个长度为 $n-2$、每项属于 $[1,n]$ 的序列也对应唯一的带标号树。
- 点 $i$ 的度数等于它在 Prüfer 序列中的出现次数加 $1$。
- Cayley 公式：$n$ 个带标号点可以组成 $n^(n-2)$ 棵树。
- 给定正整数 $d_1,d_2,dots,d_n$。如果 $sum_(i=1)^n d_i=2 n-2$，度数依次为这些数的带标号树共有

  $ frac((n-2)!, product_(i=1)^n (d_i-1)!) $

  棵；如果条件不成立，则不存在这样的树。

最后一个公式来自序列中的出现次数：点 $i$ 必须出现 $d_i-1$ 次，因此只需计算这些项的排列数。
